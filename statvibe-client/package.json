{
  "name": "statvibe-client",
  "version": "1.0.0",
  "private": true,
  "description": "StatVibe mobile-first web app (Vite SPA). Deploy as Vercel project Root Directory: statvibe-client → https://stat-vibe.vercel.app",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
